# Disable source maps in production builds.
# Some dependencies (e.g. html5-qrcode) ship source maps that reference
# TypeScript files not included in the npm package, which produces noisy
# (harmless) build warnings. Disabling source maps also shrinks the bundle.
GENERATE_SOURCEMAP=false
